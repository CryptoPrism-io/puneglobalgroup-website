import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import { AuthProvider } from "@/context/AuthContext";
import "./globals.css";

const geistSans = Geist({ variable: "--font-geist-sans", subsets: ["latin"] });
const geistMono = Geist_Mono({ variable: "--font-geist-mono", subsets: ["latin"] });

export const metadata: Metadata = {
  title: "Pune Global Group",
  description: "Global business solutions rooted in Pune. Trusted by businesses worldwide.",
  keywords: "Pune Global Group, business solutions, consulting, Pune",
  openGraph: {
    title: "Pune Global Group",
    description: "Global business solutions rooted in Pune.",
    url: "https://puneglobalgroup.in",
    siteName: "Pune Global Group",
    type: "website",
  },
};

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="en" className="scroll-smooth">
      <body className={`${geistSans.variable} ${geistMono.variable} antialiased`}>
        <AuthProvider>{children}</AuthProvider>
      </body>
    </html>
  );
}
